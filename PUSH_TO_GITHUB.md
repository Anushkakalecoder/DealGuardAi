# Push to GitHub

From the repository root:

```powershell
git init
git add .
git commit -m "Build DealGuard AI hackathon MVP"
git branch -M main
git remote add origin https://github.com/YOUR_USERNAME/dealguard-ai.git
git push -u origin main
```

The repository intentionally contains **no `.env` files and no credentials**.
